[project]
name = "cytoolz-stubs"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "OutSquareCapital", email = "166045166+OutSquareCapital@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.packages]
cytoolz = "src/cytoolz"  # Pointe directement vers le dossier contenant les stubs

[tool.hatch.build.targets.wheel]
packages = ["cytoolz"]  # Ceci installe les stubs dans le répertoire cytoolz

[tool.hatch.build.targets.sdist]
include = ["/src", "/pyproject.toml"]

[dependency-groups]
dev = [
    "cytoolz>=1.0.1",
    "tqdm>=4.67.1",
]